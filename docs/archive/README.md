# Internal archive (not published)

This tree is **excluded from MkDocs** (`exclude_docs` in `docs/mkdocs.yml`) and from **ruff** (`exclude` in `ruff.toml`). It preserves draft work only:

| Path | Contents |
|------|----------|
| `manim/` | Draft / combined Manim scene sources (hero reel rendered to `frontend/assets/videos/`) |
| `tasks/` | Phase checklists and working notes |
| `viz_output/` | One-off Plotly/HTML experiments |
| `heroku-post_compile` | Legacy deploy hook |

Do not link these paths from the public Adaptive Efficiency site or committee packet.
